---
description: 
globs: 
alwaysApply: true
---
# Project Overview

This is a macOS mini-app to collect and save YouTube/Twitter links for watching later, avoiding browser tab overload.

The project is structured as a **TurboRepo monorepo** with two packages:

## 📦 `api/`
- Database: **Convex**
- Backend framework: **ElysiaJS**
- API layer: **tRPC**

## 🧩 `app/`
- Desktop app: **Tauri v2**, built with **Vite** and styled using **TailwindCSS**
- Data fetching: **TanStack Query**
- UI components: **Shadcn**
- Client-side routing: **TanStack Router**

---

# Coding & Comments

- **All code and comments MUST be written in English**, regardless of communication language.
- Use consistent code style across `api/` and `app/`, following your usual TypeScript + Tailwind conventions.
- When referring to “the app”, “the project”, or “the application”, treat it as this specific macOS link-saver.

---

# Usage Guidelines

- Apply this rule globally across the project: `alwaysApply: true`
- Cursor will automatically include this context in all chat and inline edit interactions.
- Use this context to generate code, create new features, or refactor existing logic.